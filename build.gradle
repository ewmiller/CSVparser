apply plugin: 'idea'
apply plugin: 'java'
task runApp(type: JavaExec){
    classpath = sourceSets.main.runtimeClasspath

    //tell gradle where to find the main class
    main = 'main.java.Main'

    //arguments for the app - this is where the filename will go
    args 'hey'
}
